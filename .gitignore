# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets
.env
backend/.env

# Backend game database (created at runtime)
*.db
*.sqlite
*.sqlite3

# Python
__pycache__/
*.pyc

# Per-batch bird-photo working files (regenerated/overwritten each batch)
batch_input.json
batch_photos.json

# Local account state dumps/backups from one-off live-data edits (never commit)
marnich_*.json
*_state_raw.json
*_backup_pre_*.json
backups/

# Local preview/working output
previews/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Claude Code local tooling (session config, installed skills) -- not part
# of the product, machine/session-specific, never commit
.claude/
.agents/
skills-lock.json
